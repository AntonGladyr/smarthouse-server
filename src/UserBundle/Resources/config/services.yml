services:
#    user.example:
#        class: UserBundle\Example
#        arguments: ["@service_id", "plain_value", %parameter%]
    user.provider:
        class: userBundle\UserProvider
        arguments: [@users.access]